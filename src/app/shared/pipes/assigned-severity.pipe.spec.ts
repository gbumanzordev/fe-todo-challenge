import { AssignedSeverityPipe } from './assigned-severity.pipe';

describe('AssignedSeverityPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedSeverityPipe();
    expect(pipe).toBeTruthy();
  });
});
