import { ElapsedDaysPipe } from './elapsed-days.pipe';

describe('ElapsedDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new ElapsedDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
