export enum Status {
  BACKLOG,
  CANCELLED,
  DONE,
  IN_PROGRESS,
  TODO,
}

export enum TaskTag {
  ANDROID,
  IOS,
  NODE_JS,
  RAILS,
  REACT,
}

export enum UserType {
  ADMIN,
  CANDIDATE,
}

export enum PointEstimate {
  EIGHT,
  FOUR,
  ONE,
  TWO,
  ZERO,
}
